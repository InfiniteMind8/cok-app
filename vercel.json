{
  "regions": ["iad1"],
  "buildCommand": "pnpm exec prisma generate && pnpm next build",
  "installCommand": "pnpm install --frozen-lockfile",
  "crons": [
    { "path": "/api/cron/leases", "schedule": "0 1 * * *" }
  ]
}
