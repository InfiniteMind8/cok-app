# City of Karis — Community App — Environment Variables
# Copy this to .env.local and fill in your values.

# --- Supabase PostgreSQL ---
# Get from: https://supabase.com → Project Settings → Database
# Use the "Connection Pooler" string for DATABASE_URL (port 6543, pgbouncer=true)
# Use the "Direct connection" string for DIRECT_URL (port 5432)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/postgres"

# --- Clerk Authentication ---
# Get from: https://dashboard.clerk.com → Your App → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
CLERK_SECRET_KEY="sk_test_..."
CLERK_WEBHOOK_SECRET="whsec_..."

# Clerk redirect configuration (these values work with the current route structure)
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"

# --- Resend (transactional email) ---
# Get from: https://resend.com/api-keys
# See docs/email-setup.md for full DNS verification and domain setup instructions
RESEND_API_KEY="re_..."
RESEND_FROM_EMAIL="noreply@cityofkaris.com"
RESEND_FROM_NAME="City of Karis"

# --- Uploadthing (file uploads — legacy; superseded by StorageDriver in D.11) ---
# Still required if any UploadThing components are referenced during build.
# Can be removed once all uploads are fully migrated to STORAGE_DRIVER.
UPLOADTHING_TOKEN="eyJ..."

# --- Storage (D.11) ---
# Driver selection: "local" for development (encrypted filesystem) or "s3" for production.
STORAGE_DRIVER="local"

# AES-256-GCM key for local driver encryption and signed-URL HMAC.
# Generate with: openssl rand -hex 32
# Must be exactly 64 hex characters (32 bytes). Keep this secret.
STORAGE_ENCRYPTION_KEY=""

# S3-compatible storage (required when STORAGE_DRIVER=s3)
# Backblaze B2, Cloudflare R2, or AWS S3 are all supported.
STORAGE_S3_BUCKET=""
STORAGE_S3_REGION="us-east-1"
# Leave STORAGE_S3_ENDPOINT empty for AWS S3; set for B2/R2
# e.g. https://s3.us-east-1.backblazeb2.com  or  https://ACCOUNT_ID.r2.cloudflarestorage.com
STORAGE_S3_ENDPOINT=""
STORAGE_S3_ACCESS_KEY=""
STORAGE_S3_SECRET_KEY=""

# --- Demo mode ---
# Set to "true" to show six one-click demo-account buttons on the sign-in page.
# Set to "false" (or omit) for production — no demo elements are rendered at all.
NEXT_PUBLIC_DEMO_MODE_ENABLED="false"

# --- Public demo showcase (PRE-3) ---
# Set to "true" to expose a public, no-login UX/UI tour at /demo with five
# persona sub-routes (master-admin, admin, resident, vendor, visitor). All
# data is hardcoded fixtures (lib/demo/fixtures.ts); no DB queries, no
# Server Actions, no email sends. Set to "false" (or omit) post-launch and
# /demo/* will return 404. Independent of NEXT_PUBLIC_DEMO_MODE_ENABLED.
NEXT_PUBLIC_DEMO_SHOWCASE_ENABLED="true"

# --- Sentry Error Monitoring ---
# Get from: https://sentry.io → Project Settings → Client Keys (DSN)
# SENTRY_DSN is used server-side; NEXT_PUBLIC_SENTRY_DSN is used client-side.
SENTRY_DSN="https://examplePublicKey@o0.ingest.sentry.io/0"
NEXT_PUBLIC_SENTRY_DSN="https://examplePublicKey@o0.ingest.sentry.io/0"

# Deployment environment tag (development | staging | production)
DEPLOY_ENV="development"
NEXT_PUBLIC_DEPLOY_ENV="development"

# Git SHA for release tagging — set automatically in CI (e.g. $VERCEL_GIT_COMMIT_SHA)
# Leave blank for local dev (falls back to "dev")
SENTRY_RELEASE=""
NEXT_PUBLIC_SENTRY_RELEASE=""

# Auth token for source map upload (CI only — not needed for runtime)
# Get from: https://sentry.io → Settings → Auth Tokens
# Scope: project:releases, org:read
SENTRY_AUTH_TOKEN=""

# Sentry org and project slugs (used by withSentryConfig for source map upload)
SENTRY_ORG="your-org-slug"
SENTRY_PROJECT="cok-community-app"

# --- Rate limiting (Upstash Redis) ---
# Get from: https://console.upstash.com → Create database → REST API
# IMPORTANT: Without these vars, the rate limiter falls back to an in-memory store
# (process-scoped, resets on restart). In-memory limits do NOT enforce across
# edge middleware isolates in production — Redis is required for reliable auth rate limiting.
UPSTASH_REDIS_REST_URL="https://YOUR_DB.upstash.io"
UPSTASH_REDIS_REST_TOKEN="AXXXxxxxxxxxxxxxxxxxxxxxxxxxxx"

# --- App URL ---
# Public origin used to build absolute links in emails and audit log entries.
# Set to your deployed origin in staging/prod (e.g. "https://app.cityofkaris.com").
# Used by: server actions (accounts, imports, voucher-requests, settlements,
# property-transfers, rental-extensions, data-directory) and the leases cron.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --- Bulk import limits ---
# Maximum rows accepted per .xlsx upload (members + properties imports).
# Default is 1000 if unset; raise only when the underlying transactional
# pattern has been re-tested at the new ceiling.
IMPORT_MAX_ROWS="1000"

# --- Cron security ---
# Shared secret required as Bearer token by /api/cron/* routes (leases,
# reconciliation). Generate with: openssl rand -hex 32
# Must match the value configured on the cron scheduler (Vercel cron, etc.).
CRON_SECRET=""
