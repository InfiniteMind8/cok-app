# Risk Register — Phase 1+

| Risk ID | Description | Impact | Likelihood | Owner | Mitigation | Status |
|---|---|---|---|---|---|---|
| R-001 | RESEND_FROM_EMAIL domain not yet verified in Resend dashboard | High — emails silently fail or land in spam | High — DNS setup requires Dr. Munroe action | Dr. Munroe | Documented in `/docs/email-setup.md`; service logs `failed` row with provider error | Open |
| R-002 | Prisma migration requires DB access (Supabase) | Medium — schema changes can't land without connected DB | Low — local migration file created, applied on deploy | Dev | Use `prisma migrate deploy` on connected env; migration file committed to repo | Open |
| R-003 | `@react-email/components` React 19 compatibility | Low — packages are tested against React 19 | Low | Dev | Verified peer deps; render runs server-side only | Open |
| R-004 | A.2 migration `20260428000002_a2_fee_schedule_history` not applied to live DB | Medium — `effectiveTo` column and `AuditLog` table missing until applied | High — requires connected Supabase session | Dev | Migration SQL committed; run `pnpm exec prisma migrate deploy` on next connected session | Open |
| R-005 | A.3 migration `20260428000003_a3_approvals_center` not applied to live DB | Medium — `PropertyTransferRequest` and `VoucherRequest` tables missing until applied | High — requires connected Supabase session | Dev | Migration SQL committed; run `pnpm exec prisma migrate deploy` on next connected session | Open |
| R-006 | `@base-ui/react` Dialog backdrop dismiss — clicking outside must NOT close confirmation/alert-pattern modals | High — silently closes a modal the user intended to force-respond to | Low — controlled by `dismissOnBackdrop={false}` prop on ModalContent | Dev | All former AlertDialog uses (RestoreAccountDialog, CloseVoteButton) confirmed `dismissOnBackdrop={false}`; covered by structural tests | Open — verify on live browser |
| R-007 | `property-tabs-client.tsx` contained 3 dialog components (AddInstallment, AssignOwner, AssignTenant); risk was missing instances during migration | Medium — unclosed Dialog import would leave a grepped match in app/ | Resolved — all 3 components confirmed migrated in B.2 | Dev | `grep "from '@/components/ui/dialog'" app/` → zero matches | Resolved |
| R-008 | B.3 migration `20260428000004_b3_intake_forms` not applied to live DB | High — new columns (User KYC fields, Property specs, Issue title/location, PropertyTenancy dates) + new tables (VisitorProfile, VendorProfile, Attachment) missing | High — requires connected Supabase session | Dev | Migration SQL committed; run `pnpm exec prisma migrate deploy` on next connected session | Open |
| R-009 | UploadThing CDN URLs are not short-lived signed URLs as §3.4 requires | Medium — attachment URLs are long-lived CDN URLs; no per-user access control at CDN level | Low — URLs are UUIDs, not guessable; admin access logged in AuditLog | Dev | D-015 documents divergence; `getAttachmentUrlAction` enforces auth before returning URL; full §3.4 compliance (signed URLs) addressed in D.11 | Open |
| R-C2-01 | `CommunityUpdate` is named "Announcement" in the playbook; renaming would be a breaking migration | Low — terminology gap is cosmetic | N/A — decision made | Dev | D-018: extend `CommunityUpdate` in-place with targeting fields; no rename | Resolved |
| R-C2-02 | No `/admin/visitors/` route existed before C.2; entire subtree is new | Low — followed existing `/admin/accounts/` patterns; no legacy code to break | Low | Dev | New pages created under `app/(admin)/admin/visitors/groups/`; matches admin page conventions | Resolved |
| R-C2-03 | Migration `20260428215229_add_visitor_groups` not applied to live DB | High — `VisitorGroup`, `VisitorGroupMembership` tables + `AnnouncementTargetType` enum missing until applied | High — requires connected Supabase session | Dev | Migration SQL committed; run `pnpm exec prisma migrate deploy` on next connected session | Open |
| R-C2-04 | Playwright tests cannot run without a live dev server + seeded DB | Low — no regression; E2E coverage deferred per precedent | N/A — scoped to D.10 | Dev | `tests/e2e/c2-visitor-groups.spec.ts` created with `describe.skip` and full scenario documentation | Resolved |
| R-C3-01 | Existing `PropertyTenancy.cycle` is freeform String ("monthly") — not auto-mapped to new `CycleUnit` enum | Low — cosmetic mismatch; cron reads `cycleUnit` (new enum, defaults MONTHLY) | Low — resolved by design | Dev | New `cycleUnit` field backfills to MONTHLY via migration default; `cycle` String retained for display compatibility; D-021 | Resolved |
| R-C3-02 | Migration `20260429000001_c3_rental_cycle` not applied to live DB | High — `CycleUnit`/`LeaseStatus` enums, `RentalExtensionRequest` table, and new `PropertyTenancy` columns missing until applied | High — requires connected Supabase session | Dev | Migration SQL committed; run `pnpm exec prisma migrate deploy` on next connected session | Open |
| R-C3-03 | Date timezone handling in cron — `new Date()` is local-timezone if not set to UTC | Low — `date-fns` operates on JS Date objects; server likely runs UTC | Low | Dev | `today.setUTCHours(0,0,0,0)` normalizes to UTC midnight; all comparisons UTC-safe | Resolved |
| R-C3-04 | `CRON_SECRET` env var not configured → cron returns 500 | Medium — endpoint responds with 500 instead of being silently unused | Low — only called by Vercel scheduler which supplies the secret | Dev | Added to `.env.example`; endpoint returns 401 on missing/wrong secret, 500 if env not set at all | Open |
