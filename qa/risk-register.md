# Phase 1+ Risk Register

| Risk ID | Description | Impact | Likelihood | Owner | Mitigation | Status |
|---|---|---|---|---|---|---|
| R-A1-01 | Resend domain not verified | High | Medium | Project owner | DNS records documented in `/docs/email-setup.md` | Open — requires domain DNS update |
| R-C1-01 | `prisma migrate dev` requires live DB | Medium | Low | Build agent | Session pooler URL in `.env`; migration name documented | Open |
| R-C1-02 | New TransactionType enum values (FIAT_CONVERSION, CONVERSION_BONUS) may break fee-engine tests | Medium | Low | Build agent | Check exhaustive switches; handle new types with zero fee | Mitigated |
| R-C1-03 | Display layer migration regressions in existing components | Medium | Low | Build agent | Change is additive — new optional props with defaults; existing callers unchanged | Mitigated |
| R-C1-04 | Conversion engine ledger balance violation | High | Low | Build agent | Zero-sum enforced by posting debit from fiat_settlement wallet = credit to user; tested | Mitigated |
| R-B3-01 | File uploads require live UploadThing key | Medium | Medium | Project owner | Uploads tested with real key; URL-based fallback for local dev | Open |
| R-D1-01 | Clerk invitation rate limits on bulk commit (≤1,000 rows) | Low | Low | Build agent | Invitations sent sequentially; founding cohort is small; Clerk standard tier supports this | Mitigated |
| R-D1-02 | Server action FormData size limit for .xlsx upload | Low | Low | Build agent | Next.js 16 default 4MB body limit; 1,000-row xlsx is <500KB in practice | Mitigated |
| R-D1-03 | prisma migrate dev advisory lock timeout when run in background | Low | Low | Build agent | Resolved: stopped background task, applied migration manually via migrate deploy; tables confirmed present | Resolved |
| R-D2-01 | UTApi server-side upload requires UPLOADTHING_TOKEN in env | Medium | Low | Build agent | Skip zip upload with console.warn when token absent; zip metadata stored as {} | Mitigated |
| R-D2-02 | jszip extraction of large zip (>50MB) blocks event loop | Low | Low | Build agent | Upload validation rejects files >50MB with user-facing error | Mitigated |
| R-D2-03 | property code uniqueness conflict: external_ref matches existing property | Low | Low | Build agent | Per-row duplicate WARNING; admin confirms before committing | Mitigated |
| R-D3-01 | Clerk Management API call per admin page load adds ~50–100ms latency | Low | Low | Build agent | Acceptable for founding cohort; cache with unstable_cache if needed in future | Mitigated |
| R-D3-02 | Clerk backup codes require TOTP_OR_BACKUP_CODE strategy enabled in Clerk dashboard | Medium | Medium | Project owner | Documented in docs/mfa.md as setup requirement; enrollment page calls createBackupCode() which will fail without dashboard config | Open — requires Clerk dashboard setup |
| R-D4-01 | ZIP export fetches attachment binaries from UploadThing CDN URLs; requires network | Low | Low | Build agent | Per-attachment try/catch; failures recorded in manifest with status=error; export continues | Mitigated |
| R-D4-02 | Clerk disableUserMFA() requires Clerk Management API key | Medium | Low | Project owner | Same key used in D.3 requireMfaEnrolled; already in env; error returned to caller if Clerk call fails | Mitigated |
| R-D5-01 | prisma migrate dev fails with shadow DB when earlier migration (20260429044638_add_import_session_metadata) hadn't applied | Medium | Low | Build agent | Wrote migration SQL manually; applied with `prisma migrate deploy`; tables confirmed present — same pattern as D.1/D.4 | Resolved |
| R-D6-01 | Resend rate limit if broadcast is sent to a large community (>100 users/sec) | Low | Low | Build agent | Chunked sending 50/batch with 20ms delay between chunks; well within Resend limits; founding cohort is small | Mitigated |
| R-D7-01 | Tour target element not in DOM on current page | Low | Medium | Build agent | querySelector returns null → spotlight centers in viewport; tour remains usable; all step targets confirmed present in sidebar/tab-bar chrome | Mitigated |
| R-D7-02 | VENDOR/ADMIN roles have no dedicated layout; tour cannot auto-show | Low | Low | Build agent | Steps defined; TourProvider only wired in admin/resident layouts; tour deferred for those roles; documented D-D7-01 | Open — deferred |
| R-D8-01 | @sentry/cli build scripts blocked by pnpm (source map upload disabled) | Low | Low | Project owner | Added @sentry/cli to pnpm onlyBuiltDependencies; run `pnpm approve-builds` in production CI environment; source maps optional for runtime error capture | Open — requires pnpm approve-builds in CI |
| R-D8-02 | SENTRY_AUTH_TOKEN absent in local dev suppresses source map upload silently | Low | Low | Build agent | silent: !process.env.SENTRY_AUTH_TOKEN ensures no error when token absent; source maps uploaded only in CI with token set | Mitigated |
| R-D9-01 | InMemoryRatelimit in edge middleware does not persist across isolates | Medium | High (prod without Redis) | Project owner | Auth/MFA rate limits only enforce reliably with Redis configured; in-memory is per-process (dev only); documented in .env.example | Open — requires UPSTASH_REDIS_REST_URL in production |
| R-D9-02 | @upstash/ratelimit postinstall scripts blocked by pnpm | Low | Low | Build agent | Checked: @upstash/ratelimit@2.0.8 has no native postinstall scripts; no addition to onlyBuiltDependencies required | Mitigated |
| R-D10-01 | Demo sign-in requires real Clerk instance with demo user IDs configured; CI needs CLERK secrets | High | Medium | Project owner | Documented in .env.test.example; CI workflow uses repo secrets; demo buttons call /api/auth/token which requires CLERK_SECRET_KEY | Open — requires GitHub secrets setup |
| R-D10-02 | Test 2 (ledger-transfer) blocked — no resident-to-resident KCRD transfer UI in Phase 1+ | Medium | Certain | Build agent | test.skip with TODO; TRANSFER type in schema + fee schedule; gap documented in docs/testing.md | Open — deferred |
| R-D10-03 | TOTP secret extraction in mfa-enrol test depends on Clerk test instance supporting TOTP; createTOTP() fails without dashboard config | Medium | Medium | Project owner | Test exits gracefully with console.warn when secret not extractable; enrollment test proceeds to whatever state is reachable | Open — requires Clerk dashboard MFA config |
| R-D10-04 | E2E tests share seeded DB state; a passing test (approve request) removes the fixture for re-run | Low | Low | Build agent | global-setup uses upsert/findFirst so re-running global-setup before tests recreates fixtures; document in testing.md | Mitigated |
| R-D11-01 | STORAGE_ENCRYPTION_KEY must be exactly 64 hex chars (32 bytes) | High | Low | Project owner | Validated at LocalStorageDriver construction; throws informative error with env var name; Generate: `openssl rand -hex 32` | Mitigated |
| R-D11-02 | Node crypto module not available in Edge runtime | Medium | Low | Build agent | `/api/attachments/serve` and `/api/attachments/upload` both export `runtime = 'nodejs'`; no Edge runtime used | Mitigated |
| R-D11-03 | S3 driver unit tests must mock AWS SDK — no real network calls | Low | Low | Build agent | `vi.mock('@aws-sdk/client-s3', ...)` with constructible class mock; `vi.mock('@aws-sdk/s3-request-presigner', ...)` — all 6 S3 tests pass | Mitigated |
| R-D11-04 | FileUpload component interface changed from UploadThing string endpoint to UploadEndpointConfig object | Medium | Low | Build agent | All 6 call sites updated; build clean with TypeScript strict; no remaining UploadThing imports in upload path | Mitigated |
| R-D11-05 | Existing Attachment rows have no sha256; migration defaults sha256="" encrypted=false | Low | Certain | Build agent | Migration uses NOT NULL DEFAULT '' for sha256/category and DEFAULT false for encrypted; safe for existing rows | Mitigated |
| R-D11-06 | shadow DB migration issue — use `prisma migrate deploy` if `migrate dev` fails | Low | Low | Build agent | Migration applied via `prisma migrate deploy`; consistent with D.1/D.5 pattern | Resolved |
| R-D12-01 | Restore drill cannot be executed by build agent (requires live Supabase console access) | Medium | Certain | Project owner | Drill procedure documented in full in `docs/backup-and-restore.md`; project owner must execute before production launch; AC2 marked partially met | Open — requires project owner action |
| R-D12-02 | Supabase free plan retains only 1 daily backup (1-day window) — insufficient for 14-day target | Medium | High (if on free plan) | Project owner | Documented in runbook; upgrade to Pro plan required before go-live; Pro provides 7-day PITR | Open — upgrade required for production |
| R-D12-03 | Loss of STORAGE_ENCRYPTION_KEY makes all local-driver files permanently unrecoverable | High | Low (if key is backed up) | Project owner | Key management section in runbook (§4) requires key stored in password manager + off-site; generate: `openssl rand -hex 32` | Open — key backup is operational responsibility |
| R-D14-01 | System wallets seeded with floor_kcrd=null by default — floor protection inactive until explicitly configured | Medium | High (initial state) | Project owner | All system wallets start with null floor (unlimited overdraft). Master Admin must set floors in /admin/treasury before go-live to activate protection | Open — floor config is operational responsibility |
| R-D14-02 | Prisma generate must be re-run after schema changes in environments that cache the generated client | Low | Low | Build agent | Resolved in this session: `prisma generate` run after migration; build confirmed clean. Pattern documented. | Resolved |
| R-E2-01 | xlsx@0.18.5 has Prototype Pollution (GHSA-4r6h-8v6p-xvw6) and ReDoS (GHSA-5pgg-2g8v-p4x9) vulnerabilities; no free npm patch exists (SheetJS moved fixes to paid Pro tier) | High | Low (admin-only upload surface) | Build agent | Compensating controls: MASTER_ADMIN-only upload (`requireRole(['MASTER_ADMIN'])`), rate limit 3/hr/user, file hash computed before parse, 64MB size cap. Upgrade path: evaluate replacing xlsx with exceljs or equivalent when xlsx Pro fix is available or when resourcing allows migration. | Open — no npm patch available; accepted with compensating controls |
| R-E2-02 | effect <3.20.0 (GHSA-38f7-945m-qr2g) transitive via @uploadthing/react>@uploadthing/shared — AsyncLocalStorage context contamination in concurrent Effect fibers | High | Low (not invoked by @uploadthing in our usage pattern) | Build agent | @uploadthing used only for server-side UTApi zip upload (D.2) and legacy route handler — no concurrent Effect fiber orchestration. Monitor @uploadthing releases for update that ships effect ≥ 3.20.0. | Open — pending @uploadthing upstream update |
| R-E2-03 | Dev-only transitive moderate vulnerabilities: hono <4.12.16 (via prisma>@prisma/dev), @hono/node-server <1.19.13 (via prisma>@prisma/dev), postcss <8.5.10 (via next>postcss), ip-address ≤10.1.0 (via shadcn>@modelcontextprotocol/sdk) | Medium | Low (dev/build-time only; not in production runtime) | Build agent | All paths are dev-only or build-time; not reachable in production runtime. Will resolve naturally when prisma, next, and shadcn release updates. Monitor `pnpm audit` on subsequent sessions. | Open — pending upstream updates |
| R-E2-04 | Content-Security-Policy not yet set — deferred to F.1 | Medium | Medium (XSS mitigation layer absent until F.1) | Build agent | Five other security headers (HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy) are in place. CSP enumeration requires Clerk/Sentry/storage domain audit — designated to F.1 step 3 per playbook. | Resolved 2026-05-09 — CSP added in F.1 (`website/next.config.ts`); allowlist composition recorded D-F1-01; verified live via `qa/headers-scan-f1.txt` |
| R-E3-01 | `karis-stone-500` token (#978d82 ≈ oklch(0.65 0.02 70)) on white background — 3.25:1 contrast ratio fails WCAG SC 1.4.3 (4.5:1 required for text ≤18pt) | Medium | Certain (token used in multiple components) | Build agent | Two highest-impact instances fixed in E.3 (sign-in ghost button, sign-in footer link — both changed to `karis-stone-700`). Remaining usages on authenticated pages (admin dashboard stat labels, resident community timestamps) are secondary/decorative text at small size. All remaining usages should be migrated to `karis-stone-700` in a future pass. | Open — partial fix in E.3; remaining usages tracked for E.4/F.1 |
| R-E3-02 | `status-red` / `text-status-red` color borderline contrast on light `karis-stone-50` card backgrounds | Low | Low (only rendered on error state text) | Build agent | Error text in forms renders on white (#ffffff) background; contrast acceptable. Only a risk if rendered on the stone-50 panel background. Verified acceptable in common rendering paths. | Mitigated — acceptable in observed paths |
| R-E3-03 | iOS Safari input zoom: Safari auto-zooms inputs with font-size < 16px; removing `user-scalable=no` (FIX-01) allows this zoom but does not fix it | Low | Medium (mobile users may experience zoom on form inputs) | Build agent | FIX-01 removed `user-scalable=no` to comply with WCAG 1.4.4. The iOS zoom on small inputs is a separate UX concern. Inputs use 14px font-size (`text-sm`). Workaround: set font-size ≥ 16px on mobile viewpoints via responsive class. Deferred to E.4/F.1. | Open — iOS zoom on small inputs is a known pattern; deferred |
| R-F1-01 | Pre-existing PRE-3 lint regression: `app/access/callback/_components/access-callback-client.tsx:14:20` triggers `react-hooks/set-state-in-effect` (1 error). Note: a second baseline warning was reported here originally, but inspection of `qa/f1-lint.txt` shows it lives in `app/(admin)/_actions/imports.ts` (createAuditEntry unused) and is unrelated to PRE-3. | Low | Certain | Build agent | Closure-session fix in `ff23fa7`: lifted `searchParams.get('ticket')` to a top-level constant; replaced the single `error` state with `asyncError` set only from the async catch block; the "no ticket" case is now derived at render time. `pnpm lint` 0 errors (was 1); imports.ts warning unchanged. | Resolved 2026-05-09 — fixed in `ff23fa7` |
| R-F1-02 | F.1 commit on `.env.example` bundles one-line PRE-3 addition (`NEXT_PUBLIC_DEMO_SHOWCASE_ENABLED`) that was in the working tree pre-F.1 | Low | Certain | Build agent | The PRE-3 line was already present uncommitted; rather than discard the owner's WIP or split via interactive add, F.1 bundles it. Disclosed in the F.1 handover and progress note. | Mitigated — disclosed |
| R-F1-03 | Smoke flows (sign-in, transfer, admin) and `securityheaders.com` A-grade scan require deployed staging URL | Medium | Certain | Project owner | Documented in `website/docs/production-deploy.md` §3.3 + §5. Procedure listed; owner runs against staging or production after first deploy. Precedent: D-D12-01 (restore drill). | Open — requires project owner action |
| R-F1-04 | Two `qa/phase1plus-progress.md` trackers exist (root `qa/` outside git vs. `website/qa/` inside git); they have diverged | Low | Certain | Build agent | Closure-session resolution in `04982a6`: `website/qa/` deleted entirely (4 stale tracked .md files + `codex-review/` untracked subdir); root `COK-City-of-Karis/qa/` is canonical going forward. Decision affirmed by user during the closure session AskUserQuestion (option: "Delete website/qa/"). | Resolved 2026-05-09 — fixed in `04982a6` |
| R-F2-01 | Bubblewrap signed AAB build requires JDK 17+, keytool, and an Android signing keystore | High | Certain | Project owner | All non-runnable steps documented in full in `website/docs/play-store.md` §2–§6 + `website/docs/go-live.md` Phase F. Owner provisions JDK + Bubblewrap on a workstation, runs `bubblewrap init` against the live production URL, generates the upload key (passphrase stored in password manager + offline backup), runs `bubblewrap build`, smoke-installs the APK on a real Android device. Same pattern as D-D12-01 (restore drill) and R-F1-03 (smoke + securityheaders.com). | Open — requires project owner action |
| R-F2-02 | `public/.well-known/assetlinks.json` ships with placeholder SHA-256 fingerprint until `bubblewrap build` emits the upload key fingerprint | Medium | Certain | Project owner | Procedure documented in `website/docs/play-store.md` §5: extract via `keytool -list -v -keystore android.keystore -alias android` after first signing, paste into `assetlinks.json`, commit, redeploy, verify the address bar disappears in the installed TWA. Without this step the Android client shows the Chrome chrome and the Play Store reviewer fails the listing. | Open — requires project owner action post-bubblewrap |
| R-F2-03 | `legal/privacy.md` and `legal/terms.md` are author-best-effort drafts, not legally reviewed | High | Certain | Project owner | Both files open with a visible "DRAFT — pending counsel review" banner. `website/docs/go-live.md` Phase E gates the Play Store production track on counsel approval. Drafts cover Phase 1+ data flows, KCRD/Promotions Wallet handling, lease lifecycle, and visitor groups — counsel adjusts language and applicability to Guyanese law. | Open — requires counsel review |
| R-F2-04 | `sharp` install on Windows occasionally fails postinstall script approval | Low | Low | Build agent | `sharp` added to `pnpm.onlyBuiltDependencies` so its native build runs; verified working in this session (icons + feature graphic generated end-to-end). If a future agent hits a postinstall block, run `pnpm approve-builds`. | Mitigated — verified in F.2 session |
| R-F2-05 | Hand-rolled service worker can serve stale assets if the `VERSION` constant is not bumped on each prod release | Medium | Medium | Project owner / next dev session | `public/sw.js` documents the convention at the top: bump `VERSION = 'karis-vN'` per release. `activate` event evicts caches with non-matching prefixes. `website/docs/play-store.md` §9 step 4 includes the bump in the release checklist. **Closure session 2026-05-09 verification:** `.github/workflows/` contains only `e2e.yml`; no release/deploy workflow exists, so the bump remains owner-driven per release rather than CI-enforced. Inactive for the initial launch (`VERSION='karis-v1'` ships unchanged). The owner enters the bump path naturally via `play-store.md` for any future release — `go-live.md` is the initial-launch runbook and the future-release path is correctly anchored in the mobile-side doc. No additional gating needed at this time; consider a CI gate that fails when `public/sw.js` is touched without the `VERSION` constant changing as a Phase 2 hardening item. | Operational — release-checklist mitigation verified 2026-05-09; no blocker for initial launch |
